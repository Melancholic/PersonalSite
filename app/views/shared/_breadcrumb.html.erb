<div class="row">
    <div class="container">
        <div class="col-md-8">
           <% breadcrumb :category, @category %>
            <% breadcrumb :article, @article  if @article.present?%>
           <%= breadcrumbs pretext: "You are here: ",
           style: :bootstrap %>
       </div>
   </div>
</div>