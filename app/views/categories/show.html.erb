<div class="row">
    <div class="container">
        <div class="col-md-8 center-block">
        <%= paginate @articles %>
        </div>
    </div>
</div>
<% @articles.each do |article| %>
<% if can?(:read, article)%>
<%= render article %>
<%end%>
<% end %>
<div class="row">
    <div class="container">
        <div class="col-md-8 center-block ">
        <%= paginate @articles %>
        </div>
    </div>
</div>